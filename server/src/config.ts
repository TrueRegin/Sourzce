/**
 * Shared config file accessible in both the client and the server
 * via import {} from "@C"
 */
export const DEV_IP = 'http://localhost:8081';
export const EXISTS_ROUTE = '/SERVER_EXISTS';
export const DFT_PORT = 9090;

